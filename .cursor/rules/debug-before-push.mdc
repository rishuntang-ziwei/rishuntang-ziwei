---
description: 每次修改完成後先 debug 驗證，再詢問是否 push
alwaysApply: true
---

# Debug 後再交付

每次完成程式修改，必須先 debug，再通知使用者；不要自動 push。

## 流程

1. **修改前**：理解問題，必要時先重現 bug（預覽、瀏覽器、指令輸出）。
2. **修改後**：至少做一次 debug 驗證，例如：
   - 開啟相關頁面預覽（含手機寬度若相關）
   - 執行測試或建置指令
   - 確認 PNG 匯出、列印尺寸、版面無溢出
3. **回報使用者**：
   - 說明改了什麼、debug 結果如何
   - 結尾詢問是否要 **push**，不要預設上線
4. **僅在使用者明確同意後**才 commit / push。

## 禁止

- 未 debug 就請使用者 push
- 未 debug 就自行 push（使用者已明確要求 push 時仍須先驗證）

## 範例

```
❌ 改完 CSS 後直接說「已修正，要 push 嗎？」但未開預覽
✅ 改完 CSS → 瀏覽器確認 logo 尺寸正常 → 再告知結果並問是否 push
```
